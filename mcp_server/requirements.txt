mcp>=1.0.0
mysql-connector-python>=8.0.0
numpy>=1.21.0
psutil>=5.8.0
aiofiles>=0.8.0

# HTTP transport dependencies (Phase 2)
aiohttp>=3.12.0
aiohttp-cors>=0.8.0

# Semantic search dependencies
sentence-transformers>=2.2.2
torch>=1.9.0
transformers>=4.6.0

# Phase 3 ML dependencies
scikit-learn>=1.0.0
pandas>=1.3.0
joblib>=1.1.0

# Phase 4 Advanced AI dependencies (lightweight subset for testing)
networkx>=2.8.0
Pillow>=9.0.0

# Note: Heavy AI dependencies (torch, transformers, spacy, opencv) will be added in production
# These are commented out for initial testing to avoid container build issues:
# torch>=1.9.0
# transformers>=4.21.0
# spacy>=3.4.0
# opencv-python>=4.5.0
# torchaudio>=0.9.0
# torchvision>=0.10.0